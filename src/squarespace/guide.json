{
  "version": "1",
  "steps": {
    "intro": {
      "dashboardUrl": "https://account.squarespace.com/domains",
      "visual": {
        "type": "image",
        "src": "logo.png"
      }
    },
    "dns": {
      "instructions": [
        "Click 'DNS' and then 'DNS Settings'",
        "Scroll down to 'Custom Records'",
        "Click 'Add Record'",
        "Select the record type from the dropdown menu",
        "Enter the values from the table below in the corresponding fields",
        "Click 'Save' to add the record"
      ],
      "visual": {
        "type": "image",
        "src": "dns.png"
      }
    }
  }
}
